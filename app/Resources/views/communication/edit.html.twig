{% extends 'base.html.twig' %}

{% block title %}Communications{% endblock %}

{% block page_title %}<h1 class="mytitle">Modification</h1>{% endblock %}
{% block page_subtitle %}{% endblock %}

{% block body %}
    {% block page_content %}
        {# <h3>Modification</h3> #}

            <a class="button btn btn-primary" style="width:120px;margin:0px;" href="{{ path('communication_index') }}">Retour</a>
            <input class="button btn btn-primary" style="width:120px;margin:0px;" type="submit" value="Enregistrer" />
        {# {{ form_start(edit_form) }} #}
    {# <div class="row">
        <div class="col-md-3" >
            <div>{{form_row(form.type)}}</div>
        </div>
        <div class="col-md-3">
            {{form_row(form.nif)}}
        </div>
        <div class="col-md-3">
            {{form_row(form.rs)}}
        </div>
        <div class="col-md-3" >
            <div>{{form_row(form.contact)}}</div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-6">
            {{form_row(form.objet)}}
        </div>
        <div class="col-md-6">
            {{form_row(form.observation)}}
        </div>
        {# <div class="col-md-4">
            {{form_row(form.utilisateur)}}
        </div> #}
    </div> #}

            {{ form_widget(edit_form) }}
        {# {{ form_end(edit_form) }} #}


                {# {{ form_start(delete_form) }} #}
                    {# <input type="submit" value="Delete"> #}
                {# {{ form_end(delete_form) }} #}
    {% endblock %}
{% endblock %}
