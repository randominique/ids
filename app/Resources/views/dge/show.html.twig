{% extends 'base.html.twig' %}

{% block body %}
    <h1>Dge</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dge.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('dge_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('dge_edit', { 'id': dge.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
