{% extends "base.html.twig" %}
{% block title %}Déclarations{% endblock %}

{% block page_title %}
<h1 class="mytitle" >Déclarations</h1>
{% endblock %}
{% block page_subtitle %}{% endblock %}

{% block page_content %}

<div id="nav-menu">
	{# {% if isChefSAI or isChefDeDirection or isSuperAdmin or isSystemUser %}
	{% endif %} #}
	{# <div>
			<a href="{{path('entrantExcel')}}" class="" role="button" style="width:140px;margin-right:4px;">Exporter</a>
	</div > #}
	<div>
		<a href="#" class="" id="search-go" style="width:140px">Rechercher</a>
	</div >
</div>

	<div class="box box-primary" style="overflow-x:auto;min-height:1000px;">
		<div class="row">
			<div class="col-lg-12">
				<div id="search">
					<form class="form-inline pull-left" method="GET">
						<div class="form-group">
							<input name="nif" type="text" class="form-control" id="nifFilter" autocomplete='off' {% if nifFilter %}value="{{ nifFilter }}"{% endif %} placeholder="NIF" style="width:110px;margin-left:0px;">
						</div>
						<div class="form-group">
							<input name="rs" type="text" class="form-control" id="rsFilter" autocomplete='off' {% if rsFilter %}value="{{ rsFilter }}"{% endif %} placeholder="Raison Sociale" style="width:250px;margin-left:0px;">
						</div>
						<div class="form-group">
							<input name="date_du" type="text" class="form-control" id="date_timepicker_start" autocomplete='off' {% if date_du %}value="{{ date_du }}"{% endif %} placeholder="Date du" style="width:120px;margin:0px;">
						</div>
						<div class="form-group">
							<input name="date_au" type="text" class="form-control" id="date_timepicker_end" autocomplete='off' {% if date_au %}value="{{ date_au }}"{% endif %} placeholder="Date au" style="width:120px;margin:0px;">
						</div>
						<button type="submit" class="btn-link"><span class="fa fa-search" style="font-size:28px" title="Rechercher"></span></button>						
					</form>
				</div>
			</div>
			<div class="col-lg-12">
				<div class="navigation">
					{{ knp_pagination_render(declares) }}
				</div>
			</div>
		</div>

		<table class="table table-striped table-hover">

			<thead>
				<tr>
					<th class="form-entete">N.I.F</th>
					<th class="form-entete">Raison sociale</th>
					<th class="form-entete">Doc_Type_No</th>
					<th class="form-entete">Created_date</th>
					<th class="form-entete">Received_date</th>
					<th class="form-entete">Doc_Tp_Start_date</th>
					<th class="form-entete">Doc_Tp_End_date</th>
					<th class="form-entete">Doc_Tp_Due_date</th>
					<th class="form-entete">Doc_Tp_Month</th>
					<th class="form-entete">Doc_Tp_Year</th>
					<th class="form-entete">Doc_State_No</th>
				</tr>
			</thead>

			<tbody>
				{% set i=0 %}
				{% set newArray = [] %}
					{% for declare in declares %}						
						{% if declare not in newArray %}
							<tr>
								<td>{{ declare.nif|number_format(0,',',' ') }}</td>
								<td>{{ declare.rs }}</td>
								<td>{{ declare.docTypeNo }}</td>
								<td style="text-align:center">{{ declare.createdDate | date('d/m/Y')}}</td>
								<td style="text-align:center">{{ declare.receivedDate | date('d/m/Y')}}</td>
								<td style="text-align:center">{{ declare.docTpStartDate | date('d/m/Y')}}</td>
								<td style="text-align:center">{{ declare.docTpEndDate | date('d/m/Y')}}</td>
								<td style="text-align:center">{{ declare.docTpDueDate | date('d/m/Y')}}</td>
								<td>{{ declare.docTpMonth }}</td>
								<td>{{ declare.docTpYear }}</td>
								<td>{{ declare.docStateNo }}</td>
							</tr>
							{% set i=i+1 %}
							{% set newArray = newArray|merge([declare]) %}
						{% endif %}
					{% endfor %}
			</tbody>
		</table>
	</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        jQuery.datetimepicker.setLocale('fr');
        jQuery('#date_timepicker_start').datetimepicker({
            format:'Y/m/d',
            onShow:function( ct ){
            this.setOptions({
                maxDate:jQuery('#date_timepicker_end').val()?jQuery('#date_timepicker_end').val():false
            })
            },
            timepicker:false
            });
            jQuery('#date_timepicker_end').datetimepicker({
            format:'Y/m/d',
            onShow:function( ct ){
            this.setOptions({
                minDate:jQuery('#date_timepicker_start').val()?jQuery('#date_timepicker_start').val():false
            })
            },
            timepicker:false
        });
    </script>
    <script>
    	$('#nifFilter').autocomplete({
	        source: "{{ path('_autocomplete_nif2') }}",
	        minLength: 1,
	        select: function(event, ui) 
	        {   
	            $('#rsFilter').val(ui.item.raisonSoncial);
	            setTimeout(function(){
	             $('#nifFilter').val(ui.item.thisNif);
	         	}, 100);
	        }
		    }).data("ui-autocomplete")._renderItem = function(ul, item) 
		    {
		        return $( "<li class='ui-autocomplete-row'></li>" )
		            .data("item.autocomplete", item)
		            .append(item.useIt)
		            .appendTo(ul);
	    	};
		$('#rsFilter').autocomplete({
	        source: "{{ path('_autocomplete_rs2') }}",
	        minLength: 1,
	        select: function(event, ui) 
	        {   
	            
				$('#nifFilter').val(ui.item.thisNif);
	            setTimeout(function(){
	             $('#rsFilter').val(ui.item.raisonSoncial);
	         	}, 100);
	        }
		    }).data("ui-autocomplete")._renderItem = function(ul, item) 
		    {
		        return $( "<li class='ui-autocomplete-row'></li>" )
		            .data("item.autocomplete", item)
		            .append(item.useIt)
		            .appendTo(ul);
	    	};
    </script>
    <script>
	    function getContribuable(nif) {
            $.ajax({
                url: "{{ path('_get_contribuable') }}" + '?nif='+nif, 
                type: "GET",             
                contentType: false,       
                cache: false,            
                processData:false,       
                success: function(data)  
                {                
                    $('#infobox').html(data);
                }
            });
        }
        $('#dbundle_courierentrant_nif').devbridgeAutocomplete({
            serviceUrl: "{{ path('_autocomplete_nif') }}",
            minChars : 2,
            onSelect: function (suggestion) {
                console.log('You selected: ' + suggestion.value + ', ' + suggestion.data);
                $('#dbundle_courierentrant_rs').val(suggestion.data);
                getContribuable(suggestion.value);
            }
        });
        $('#dbundle_courierentrant_rs').devbridgeAutocomplete({
            serviceUrl: "{{ path('_autocomplete_rs') }}",
            minChars : 2,
            onSelect: function (suggestion) {
                console.log('You selected: ' + suggestion.value + ', ' + suggestion.data);
                $('#dbundle_courierentrant_nif').val(suggestion.data);
                getContribuable(suggestion.data);
            }
        });
    </script>
	<script>
		$(".obsobs").hide();
		var openDropdown = $(".show-dropdown");
		var openDropdown2 = $(".show-dropdown2");
		
		
		var showList = false;
		var showList2 = false;
		openDropdown.each(function(){
			var assignId = $(this).attr("data-id");
			$(this).on('click',function(){
				showList = !showList;
				if (showList) {
				$("#dropdown-list-"+assignId).show();
				}else{
					$("#dropdown-list-"+assignId).hide();
				}
			})
		})		
		openDropdown2.each(function(){
			var assignId2 = $(this).attr("data-id2");
			$(this).on('click',function(e){
				{# e.preventDefault();
				console.log("rtrtrt"); #}
				showList2 = !showList2;
				if (showList2) {
				$("#dropdown-list2-"+assignId2).show();
				}else{
					$("#dropdown-list2-"+assignId2).hide();
				}
			})
		})	
			showList3 = false;
			{# var openDropdown2 = $(".show-dropdown2"); #}
		{# $("ul.dropdown-menu").each(function(){
			$(this).on('click',function(e){
				e.preventDefault();
				console.log("stop");
			})
		}); #}
		$(".btn-nonT").each(function(){
			var assignId2 = $(this).attr("data-id");
			$(this).on('click',function(e){
				{# e.preventDefault();
				console.log("uiui"); #}
				showList3 = !showList3;
				if (showList3) {
				$("#"+assignId2).show();
				}else{
					$("#"+assignId2).hide();
				}
				function bla(){
					console.log("grrrr");
					$("#caret-"+assignId2).trigger("click");
				}
				const myTimeout = setTimeout(bla, 2000);
				
			})
		})
	</script>
	<script>
		$("#search").hide();
		$(".select-attr").hide();
		
		$("#search-go").click(function(){
			$("#search").toggle();
		})
		$(document).ready(function() {
			$(".checkGestion").each(function(){
				$(this).change(function(){
					var checked = $(this).is(":checked");
					var dataS = $(this).attr('data-s');
					if (checked) {
						$('.'+dataS).show();
					}else{
						$('.'+dataS).hide();
					}
				})
			})
		});
		
		//update link 
		var btnPrint = $("#btn-print").attr('href');
		var hrefPrintOk = btnPrint+window.location.search;
		$("#btn-print").attr('href',hrefPrintOk);
	</script>
{% endblock %}