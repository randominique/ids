
{% block title %}Assujettissement{% endblock %}

{% block page_title %}Assujettissement{% endblock %}
{% block page_subtitle %}{% endblock %}

{% block page_content %}
<style>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.9.0/Chart.min.css" />
    <link rel="stylesheet" href="//code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
    <link rel="stylesheet" href="{{ asset('css/jquery.datetimepicker.min.css') }}">
    <link rel="stylesheet" href="{{ asset('css/app.css') }}" />
</style>
 
    
    <div class="box box-primary">
        {# <div id="search">
            <form class="form-inline" method="GET">
                <div class="form-group">
                            <select name="typeImpot" class="form-control" style="margin-left:11px;">
                            <option selected disabled value = "" >Type d'impôts</option>
                            {% for taxType in taxTypes %}
                                <option value = {{ taxType.id }}>
                                    {{ taxType.taxTypeDesc }}
                                </option>
                            {% endfor %}
                            </select>
                </div>
                <button type="submit" class="btn btn-default">Recherche</button>
                <a href="" class="button btn btn-default">Réinitialiser</a>
            </form>
        </div> #}
    <div style="overflow-x:auto;">
        <table class="table table-striped table-hover ">
            <thead>
                <tr style="font-weight:bold">
                    <th>NIF</th>
                    <th>Raison Sociale</th>
                </tr>
            </thead>
            <tbody>
            {% for assessment in assessments %}
                <tr>
                    <td>{{ assessment.nif }}</td>
                     <td>{{ assessment.rs }}</td>
                  
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
   
        <div class="navigation" >
            {{ knp_pagination_render(assessments) }}
        </div>
  
</div>

{% endblock %}

