{% extends "base.html.twig" %}
{% block title %}Modifier l'attribution{% endblock %}

{% block page_title %}Modifier l'attribution{% endblock %}
{% block page_subtitle %}{% endblock %}

{% block page_content %}

    <div class="box box-primary">
        
        {% for message in app.flashes('error') %}
            <p class="alert alert-danger">
                {{ message }}
            </p>
        {% endfor %}

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <a href="{{ path('d_parametres_attribution') }}" class="btn btn-default pull-right">Retour à la liste</a>
        <button class="btn btn-primary">Mettre à jour</button>
        {{ form_end(form) }}
    </div>

{% endblock %}